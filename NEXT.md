# 下次继续 · 待办清单

> 这是「蒲公英达人工作台」的续作清单。当前已接入「乾派文化」MCN 小红书刊例 277 位博主（真实数据），线上地址：https://dai06316.github.io/pugongying-workbench/

## 当前状态（已完成）

- 页面骨架 + 样式（`index.html` / `styles.css`）
- 筛选：行业 / 身份 / 内容形式 / 量级 / 人设 / 维度 / 人群 / 年龄 / 城市 / 性别 / 预算 / 昵称搜索
- 智能匹配（品类预设 + 关键词兜底）
- 打分引擎（`js/match.js`，可配置权重）+ 主线程匹配
- 结果排序 / 分页 / 详情弹窗 / 行业洞察 / 策略 / CSV 与报告导出
- MCN 刊例导入器 `scripts/import_mcn.py` + 示例数据生成器 `scripts/generate-sample-data.mjs`

## 待完善（按优先级）

### P0 数据接入（最影响“准”）
1. ✅ 已导入「乾派文化」277 位博主；待补粉丝画像（男女/年龄/城市/人群/iPhone）与近 30 天互动、笔记数。
2. 补粉丝画像（男女/年龄/城市/人群/iPhone）与近 30 天互动、笔记数。
3. 按 `js/config.js` 的 `VERTICAL_TAG_MAP` 打行业垂类 `vmask`。
4. 用真实报价÷互动量算 CPE；重算 `insights.json` 的 stats/趋势/选题。

### P1 更准
1. 接历史商单数据做 CPE 校准（区分报备/非报备价）。
2. 按品类调打分权重（`MATCH_WEIGHTS`），给不同行业配不同画像权重。
3. 博主人群包 × 品牌 TA 的向量化/相似度匹配，替代纯占比加权。

### P2 更快
1. 全量数据接入后改回 Web Worker 打分（主线程目前示例数据够快）。
2. 结果列表虚拟滚动 / 增量渲染。
3. 匹配结果缓存（相同筛选不重复算）。

### P3 更好用
1. 客户/品牌管理 + 历史项目复用。
2. 收藏夹 + 多博主对比。
3. 一键生成带品牌落款的 PDF 报告。
4. 团队协作 / 权限。

### P4 合规与运营
1. 医疗/金融等敏感行业白名单。
2. 广告文案合规提示（“最”“第一”等）。
3. 数据更新时间标注 + 过期提示。

## 下次开工建议

1. 打开 `index.html` 确认现有交互。
2. 先做 P0：拿到一份真实蒲公英刊例（哪怕是几十条试跑）→ 我帮你写清洗脚本接入。
3. 再逐项做 P1/P2。

## 关键文件速查

- 口径/权重/品类预设：`js/config.js`
- 打分引擎：`js/match.js`
- 数据字段说明：`docs/data-schema.md`
- 数据生成器：`scripts/generate-sample-data.mjs`
- 示例数据：`data/data-full.js`（script 注入，file:// 可读）
